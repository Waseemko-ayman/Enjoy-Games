import Layer from '@/components/atomic/Layer';
import Container from '@/components/organism/Container';
import React from 'react';

const AboutContent = () => {
  return (
    <Layer>
      <Container>AboutContent</Container>
    </Layer>
  );
};

export default AboutContent;
