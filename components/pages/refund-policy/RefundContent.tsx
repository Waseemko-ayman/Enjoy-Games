import Layer from '@/components/atomic/Layer';
import Container from '@/components/organism/Container';
import React from 'react';

const RefundContent = () => {
  return (
    <Layer>
      <Container>RefundContent</Container>
    </Layer>
  );
};

export default RefundContent;
