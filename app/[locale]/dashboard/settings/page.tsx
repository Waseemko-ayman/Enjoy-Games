import SettingsPage from '@/components/pages/dashboard/settings/page';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'إنجوي قيمز | الإعدادات ',
};

const Settings = () => <SettingsPage />;

export default Settings;
