import CodesPage from '@/components/pages/dashboard/codes';
import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'إنجوي قيمز | الأكواد ',
};

const Codes = () => <CodesPage />;

export default Codes;
