import OTPPage from '@/components/pages/auth/OTP';
import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Enjoy Games | OTP',
};

const OTP = () => <OTPPage />;

export default OTP;
